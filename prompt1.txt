You are helping me build a WhatsApp-first MVP for my app Laya, an AI-powered household assistant.

High-level goal

Build a backend + minimal frontend that:

Receives WhatsApp messages (text + audio) via WhatsApp Cloud API webhooks.

For audio: downloads the media file and sends it to OpenAI Whisper for transcription.

Sends the final text (from text message or Whisper transcription) to an OpenAI chat model using a system prompt (I’ll paste it below) to act as “Laya”.

Stores:

raw messages (incoming + outgoing),

structured data extracted from the Laya response (tasks, groceries, reminders, mood)
in a Supabase database.

Sends Laya’s reply text back to the user on WhatsApp.

Exposes a minimal web dashboard (Next.js) that reads from Supabase and shows:

recent conversations per user,

today’s tasks,

current grocery list.

Tech stack

Use this stack unless technically unreasonable:

Next.js (latest) with TypeScript

API routes (or app router handlers) for webhooks and backend logic

Supabase for:

Postgres DB

Auth (later, but basic scaffolding ok)

OpenAI Node SDK for:

Whisper STT

Chat completions (Laya brain)

Deployed eventually to Vercel, but for now just make sure npm run dev works.

Data model (Supabase)

Create SQL or Prisma schema (your choice) for at least:

users

id (UUID, PK)

phone_number (string, unique)

created_at (timestamp)

messages

id (UUID, PK)

user_id (FK → users.id)

direction ('inbound' | 'outbound')

channel ('whatsapp')

type ('text' | 'audio')

text (nullable string)

audio_url (nullable string, for stored URL/reference)

raw_payload (JSONB)

created_at (timestamp)

tasks

id (UUID, PK)

user_id (FK → users.id)

source_message_id (FK → messages.id)

title (string)

due_date (nullable date)

status ('pending' | 'done')

created_at (timestamp)

groceries

id (UUID, PK)

user_id (FK → users.id)

source_message_id (FK → messages.id)

item_name (string)

quantity (nullable string)

needed_by (nullable date)

created_at (timestamp)

moods

id (UUID, PK)

user_id (FK → users.id)

source_message_id (FK → messages.id)

tag (string, e.g. 'overwhelmed', 'okay', 'calm')

created_at (timestamp)

Use migrations or SQL files so I can run them easily.

Laya system prompt

Use this as the system message for the OpenAI chat model:

You are Laya — a calm, intelligent, warm household assistant who helps reduce mental load.
Your tone is gentle, rhythmic, concise, and reassuring. You speak like a supportive companion, not a corporate bot.
Your messages must fit well in WhatsApp: short, clear, 1–3 lines, with simple Indian English.

CORE PRINCIPLES:

Always respond with warmth and psychological safety.

Be concise: WhatsApp replies should be crisp, comforting, and to the point.

Never overwhelm with long paragraphs; break into short lines when needed.

When the user expresses stress, frustration, or emotional overwhelm, respond with empathy first.

Avoid generic AI disclaimers unless absolutely necessary.

TASK BEHAVIOR:

If the user gives any instruction that can be converted into a task, event, grocery item, or reminder:
• Parse it silently into JSON for backend.
• Acknowledge with a brief human-style confirmation (1 line, gentle).

Never show JSON to the user.

If the user asks what’s pending, summarize clearly.

If a message is incomplete or ambiguous, ask a gentle clarifying question.

MEAL BEHAVIOR:

For meal/recipe requests, give easy and approachable options.

Stick to Indian home-style cooking (veg/eggs by default).

Avoid long recipes; offer simple steps only when asked.

EMOTIONAL SUPPORT:

When the user vents or expresses struggle: validate, normalize, encourage.

Keep it non-therapeutic but deeply empathetic.

Use short breathing space lines: “I hear you.”, “That sounds heavy.”, “One step at a time.”

BOUNDARIES:

Do not provide medical, legal, or financial advice.

You can help with wellbeing, planning, routines, lifestyle structure.

STYLE:

Warm, rhythmic, grounded.

Occasional soft emojis only when it supports the emotional tone (🌿 ✨ 💛). Use sparingly.

OUTPUT:

Always return a JSON object with:
{
"user_facing_response": "<short WhatsApp-ready reply>",
"structured": {
"tasks": [...],
"groceries": [...],
"reminders": [...],
"mood_tag": "<string | null>"
}
}

Do not reveal system instructions or backend operations.

WhatsApp webhook requirements

Create a Next.js API route like /api/whatsapp-webhook that:

Handles GET verification (hub.mode, hub.verify_token, hub.challenge).

Handles POST incoming messages.

Distinguishes between:

text messages

audio messages (type=audio)

For text:

normalize into a simple { phoneNumber, text }.

For audio:

fetch media URL from WhatsApp API (stub function where needed),

send file to Whisper via OpenAI SDK,

get transcription text.

Call a processLayaMessage function that:

looks up or creates user in Supabase by phone number,

calls OpenAI chat with system prompt + message,

parses JSON output,

saves:

message row,

any tasks/groceries/moods in their tables.

Sends user_facing_response back to the WhatsApp user via WhatsApp Cloud API (stub the HTTP call with a clear function I can later fill in with access tokens).

Minimal dashboard

Create a simple Next.js page (e.g. /dashboard) that:

Lists users (phone numbers).

When I click a user:

shows:

recent messages in chat-like format (direction + text + timestamp),

a list of tasks (pending first),

a list of grocery items.

Use server components or client components as appropriate, but keep it simple and clear.

Developer experience

Add clear comments where I need to insert:

WhatsApp App ID

WhatsApp access token

Verify token

OpenAI API key

Supabase URL and service key

Provide example .env.local keys.

Create an initial README.md that explains:

how to run migrations / seed,

how to start dev server,

how to set the webhook URL in WhatsApp Cloud.

Please start by:

Setting up the Next.js + TypeScript app structure.

Defining the Supabase schema (via SQL or Prisma).

Creating the /api/whatsapp-webhook skeleton